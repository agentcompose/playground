# Copy to .env and fill in. Loaded automatically by `npm start` (Node --env-file).
OPENAI_API_KEY=sk-your-key-here

# Optional — any OpenAI-compatible endpoint / gateway and model id.
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
# PORT=5173
